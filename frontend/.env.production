# Production environment — frontend-only deployment (no backend required)
# All API calls gracefully fall back to demo/mock data when backend is offline.
NEXT_PUBLIC_API_URL=https://api.halal-travels.com/api
NEXT_PUBLIC_ENV=production
